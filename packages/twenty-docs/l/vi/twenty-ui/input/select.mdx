---
title: Select
image: /images/user-guide/what-is-twenty/20.png
---
<Frame>
  <img src="/images/user-guide/what-is-twenty/20.png" alt="Header" />
</Frame>

Cho phép người dùng chọn một giá trị từ danh sách các tùy chọn được xác định trước.

<Tabs>
<Tab title="Cách sử dụng">

```jsx
import { IconTwentyStar } from 'twenty-ui/display';

import { Select } from '@/ui/input/components/Select';

export const MyComponent = () => {

  return (
    <Select
      className
      disabled={false}
      label="Select an option"
      options={[
        { value: 'option1', label: 'Option A', Icon: IconTwentyStar },
        { value: 'option2', label: 'Option B', Icon: IconTwentyStar },
      ]}
      value="option1"
    />
  );
};

```

</Tab>
<Tab title="Props">


| Props | Type | Mô tả |
|-------|------|-------|
| className | string | CSS class tùy chọn để tùy chỉnh thêm kiểu dáng |
| disabled | boolean | Khi được đặt thành `true`, vô hiệu hóa tương tác người dùng với component |
| label | string | Nhãn mô tả mục đích của component `Select` |
| onChange | function | Hàm được gọi khi các giá trị được chọn thay đổi |
| options | array | Đại diện cho các tùy chọn có sẵn cho component `Select`. Đó là một mảng các đối tượng trong đó mỗi đối tượng có `value` (mã định danh duy nhất), `label` (nhãn hiển thị), và một `Icon` tùy chọn |
| value | string | Đại diện cho giá trị hiện đang được chọn. Nó phải khớp với một trong các thuộc tính `value` trong mảng `options` |




</Tab>
</Tabs>
