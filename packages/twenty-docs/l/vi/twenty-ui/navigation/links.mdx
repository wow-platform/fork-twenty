---
title: Links
image: /images/user-guide/what-is-twenty/20.png
---
<Frame>
  <img src="/images/user-guide/what-is-twenty/20.png" alt="Header" />
</Frame>

## Contact Link

Component liên kết được tạo kiểu để hiển thị thông tin liên hệ.

<Tabs>
<Tab title="Cách sử dụng">

```jsx
import { BrowserRouter as Router } from 'react-router-dom';

import { ContactLink } from 'twenty-ui/navigation';

export const MyComponent = () => {
  const handleLinkClick = (event) => {
    console.log('Contact link clicked!', event);
  };

  return (
    <Router>
      <ContactLink
        className
        href="mailto:example@example.com"
        onClick={handleLinkClick}
      >
        example@example.com
      </ContactLink>
    </Router>
  );
};
```


</Tab>
<Tab title="Props">


| Props | Type | Mô tả |
|-------|------|-------|
| className | string | Tên tùy chọn để tùy chỉnh thêm kiểu dáng |
| href | string | URL hoặc đường dẫn đích cho liên kết |
| onClick | function | Hàm callback được kích hoạt khi liên kết được nhấp |
| children | `React.ReactNode` | Nội dung hiển thị bên trong liên kết |




</Tab>

</Tabs>

## Raw Link

Component liên kết được tạo kiểu để hiển thị các liên kết.

<Tabs>
<Tab title="Cách sử dụng">

```jsx
import { RawLink } from "/navigation";
import { BrowserRouter as Router } from "react-router-dom";

export const MyComponent = () => {
  const handleLinkClick = (event) => {
    console.log("Contact link clicked!", event);
  };

  return (
    <Router>
      <RawLink className href="/contact" onClick={handleLinkClick}>
        Contact Us
      </RawLink>
    </Router>
  );
};

```


</Tab>
<Tab title="Props">


| Props | Type | Mô tả |
|-------|------|-------|
| className | string | Tên tùy chọn để tùy chỉnh thêm kiểu dáng |
| href | string | URL hoặc đường dẫn đích cho liên kết |
| onClick | function | Hàm callback được kích hoạt khi liên kết được nhấp |
| children | `React.ReactNode` | Nội dung hiển thị bên trong liên kết |




</Tab>
</Tabs>


## Rounded Link

Liên kết kiểu bo tròn với component Chip dành cho liên kết.

<Tabs>
<Tab title="Cách sử dụng">

```jsx
import { RoundedLink } from "/navigation";
import { BrowserRouter as Router } from "react-router-dom";

export const MyComponent = () => {
  const handleLinkClick = (event) => {
    console.log("Contact link clicked!", event);
  };

  return (
    <Router>
      <RoundedLink href="/contact" onClick={handleLinkClick}>
        Contact Us
      </RoundedLink>
    </Router>
  );
};
```

</Tab>

<Tab title="Props">


| Props | Type | Mô tả |
|-------|------|-------|
| href | string | URL hoặc đường dẫn đích cho liên kết |
| children | `React.ReactNode` | Nội dung hiển thị bên trong liên kết |
| onClick | function | Hàm callback được kích hoạt khi liên kết được nhấp |



</Tab>
</Tabs>

## Social Link

Liên kết mạng xã hội được tạo kiểu, hỗ trợ nhiều loại liên kết xã hội khác nhau, chẳng hạn như URL, LinkedIn, và X (hoặc Twitter).

<Tabs>
<Tab title="Cách sử dụng">

```jsx
import { SocialLink } from "twenty-ui/navigation";
import { BrowserRouter as Router } from "react-router-dom";

export const MyComponent = () => {
  return (
    <Router>
      <SocialLink
        type="twitter"
        href="https://twitter.com/twentycrm"
      ></SocialLink>
    </Router>
  );
};
```

</Tab>
<Tab title="Props">


| Props | Type | Mô tả |
|-------|------|-------|
| href | string | URL hoặc đường dẫn đích cho liên kết |
| children | `React.ReactNode` | Nội dung hiển thị bên trong liên kết |
| type | string | Loại liên kết xã hội. Các tùy chọn bao gồm: `url`, `LinkedIn`, và `Twitter` |
| onClick | function | Hàm callback được kích hoạt khi liên kết được nhấp |




</Tab>
</Tabs>
